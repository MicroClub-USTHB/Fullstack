Algorithm Switch Case
Var
    float Note;
Begin
    read(Note)

    Switch(Note) 
        case 0: 
            write("You litterly failed")
            break
        case 10: 
            write("You almost didn't succeed")
            break
        default : 
            write("You are like most of the other people")
            break
    END_SWITCH
        

END